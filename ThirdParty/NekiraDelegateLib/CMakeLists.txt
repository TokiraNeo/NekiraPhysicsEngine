# =================================
# NekiraDelegate v1.1.2
# =================================

list(APPEND CMAKE_PREFIX_PATH "${CMAKE_CURRENT_SOURCE_DIR}/lib/cmake")

find_package(NekiraDelegateLib CONFIG REQUIRED)